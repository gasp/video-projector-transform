{
  "name": "video-projector-transform",
  "version": "1.0.0",
  "description": "calibrate your DOM using a video projector",
  "scripts": {
    "dev": "parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gasp",
  "license": "ISC",
  "dependencies": {
    "tweakpane": "^3.1.1"
  },
  "devDependencies": {
    "@tweakpane/core": "^1.1.1",
    "parcel": "^2.8.2"
  }
}
